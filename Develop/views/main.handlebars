<!DOCTYPE html>
<html lang="en">
</body>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>AURA Login</title>
    <link rel="stylesheet" href="./dst/output.css">
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

<body class="bg-blue-300 flex-wrap block grid h-screen place-items-center text-center">
  <div class="border-gray-300">
    <header class="m-0 flex-wrap border-2 rounded-sm border-gray-300 bg-green-400 p-10">
      <h1 class="text-teal-700 text-center text-4x1">
        AURA
      </h1>
      {{#if logged_in}}
      <nav class="content-center ">
        <button id="logout" class="rounded-md border border-gray-300 bg-white py-2 px-3 text-sm font-medium leading-4 text-gray-700 shadow-sm hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2">logout</button>
      </nav>
      {{/if}}
    </header>
    <main>
      {{{ body }}}
    </main>
  </div>
    <footer>
        
    </footer>

    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>