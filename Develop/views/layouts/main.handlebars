<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>AURA Login</title>
  <link rel="stylesheet" href="./dst/output.css">
</head>

<body class="bg-green-300">
  <header class="m-0 flex-wrap border-4 rounded-sm border-gray-500">
    <h1 class="text-teal-700">
      AURA
    </h1>
    {{#if logged_in}}
    <nav>
      <button class="border-2 rounded border-gray-500">logout</button>
    </nav>
    {{/if}}
  </header>
  <main>
    {{{ body }}}
  </main>
  <footer>
      
  </footer>

  {{#if logged_in}}
  <script src="/js/logout.js"></script>
  {{/if}}
</body>

</html>